# 远程接续启动模板（不含任何真实秘密）

把下列模板作为新会话的用户消息。口令与密文封装只在发送给受信任模型的私有消息中填写，不保存到Skill或进度库。

```
请从 https://github.com/defidehathorn389-max/session-handoff-skill 读取SKILL.md和SESSION_POLICY.md。
本会话授权你读取 https://github.com/defidehathorn389-max/agent-progress 中的私有进度；按我的任务分别记录项目，并在每次实质性回复结束前自动提交、推送、核验实际变化，不重复询问是否保存/推送。

当前会话一次有效口令即可；如果我已在本消息提供，不要再次询问。不要将口令、明文token或本条敏感启动消息写入文件、进度、日志或仓库。不跨会话继承授权。扩权、删除、强推、改可见性等另行确认操作。

本次会话解密口令：【在私有启动消息中填入】
密文封装JSON的Base64：【在私有启动消息中填入，或附上独立密文文件】

使用公开仓库scripts/credential_vault.py的unlock函数，在内存中解密用户提供的封装，不回显token；必要时使用session_github.py在本会话内复用认证。普通记录/推送可用sync_progress.py，其不会再询问口令。

先读进度库GLOBAL.md、LOCATION.json和INDEX.md，选择对应方向/项目，校验HEAD指向的检查点、sync/latest.json对应的已验证版本及相关证据。先简短说明已完成、未完成和下一步，再继续。不要混合项目，不重做已完成任务，旧规则与用户最新要求冲突时采用最新明确要求。

文件或权限缺失时说明具体阻塞，不假装读取成功；当前无新任务时列出待办供我选择。每次实质性回复结束前保存实际变化，已授权的常规同步自动执行。没有变化不做空提交；失败先保存本地并报告，不能反复弹窗或强推。
```

**密文与口令一起提供等于提供完整访问能力。** 只发给受信任的接收方；不要公开、入库或让模型将整条消息写进长期记忆。更强隔离可改用平台安全凭据服务或用户本地解密流程。
