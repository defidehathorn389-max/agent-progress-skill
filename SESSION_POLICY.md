# 会话内只提供一次口令，以及自动保存/推送

这是用户最新明确要求，替代旧版“每个操作批次重新询问”的规则。历史检查点中的旧规则仅为历史，不得再次触发重复询问。

## 一次有效口令

1. 当前会话开始时，简短说明本会话的仓库与操作范围。用户在当前启动消息中主动给出口令，视为本会话已提供，不再询问一遍。
2. 本会话内、用户已授权范围内可在当前上下文/进程内存中复用凭据；普通读取、项目进度写入、提交、推送和校验不再按仓库、工具调用、回复轮次分别索要密码。
3. 不把口令或明文token写入文件、长期记忆、总结、Skill、进度、日志、Git配置、命令参数或公开仓库。不跨会话继承授权；新会话以用户本次启动消息或本次主动提供为准。
4. 口令错误只说明失败并等待用户主动更正，不循环弹窗。环境重建导致本会话凭据确实不可恢复时，保存本地状态并说明无法同步，不伪造成功，也不不断索要密码。
5. 新建不在计划中的仓库、修改可见性、删除数据、强推、扩大访问范围或费用操作仍需确认**操作授权**；这不等于重新索取已经提供的密码。
6. 用户可以主动撤回本会话授权；撤回后停止凭据使用并清理临时认证通道。无人值守、跨会话自动化需要另行配置的安全凭据服务，Skill不能凭空提供永久安全记忆。

## 自动记录与同步，不反复请示

- 用户已授权：在相关任务产生实际进展、决定、失败、阻塞之后，按方向/项目保存检查点，并自动提交、推送到已指定的私有进度库，核验远端版本。每次实质性回复结束前执行这个收尾；会话真正结束前再检查。
- 模型不知道用户何时关闭窗口，不能等最后一句才保存。长任务前保存启动状态，关键里程碑后及时保存。
- 没有新信息时不制造空提交；一般问答不全量归档，不保存秘密和无关隐私。不同方向分别写入，不将某项目参数推广到全局。
- 只有授权范围内的常规同步免于重复确认；网络失败、权限不足或并发冲突应先保留本地检查点、说明阻塞。不要循环重试或强行覆盖远端。
- 收尾报告保持简短：记录了什么、推送/核验是否成功、仍有什么阻塞。不要每次弹出“是否保存/是否推送”。

## 远程冷启动

公开规则入口与私有进度入口分开。私有库里的token密文不能在没有访问凭据时自行下载，否则形成循环依赖。

可行启动方式：用户在**当前私有启动消息**提供本次口令及既有密文封装，或提供独立的密文文件。使用公开的`credential_vault.py`按封装中的AES-256-GCM+scrypt格式在内存解密；不回显token，再读取私有进度库。

公开Skill只说明这种协议，不保存用户真实密文或口令。持久化的可复制指令模板必须使用占位符。若用户明确要求在聊天中生成已填口令的启动消息，应提醒：**密文与口令放在同一消息，等于向消息接收者提供完整访问能力**。这种消息不可提交仓库或当作普通公开说明。

## 验证与回执

检查点指纹与证据、载荷提交和同步回执分别记录。回执只证明其列出的检查点，不为后来未推送的修改背书。只有实际读取远端并校验成功，才标记已同步。
